#include<lpc214x.h>
void delay(){
	int i;
	for(i=0; i<100000; i++);
}
int main(){
		IODIR0 = (1<<4)|(1<<6);
	  while(1){
			
			IOSET0 = (1<<4)| (1<<6);
			delay();
			IOCLR0 = (1<<4)| (1<<6);
			delay();
			
		}
	return 0;
}